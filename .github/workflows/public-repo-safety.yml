name: Public repository safety

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  secret-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject tracked vendor binaries and local credentials
        run: ./scripts/check-public-repo.sh
